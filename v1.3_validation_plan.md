# v1.3 Validation Plan

编制日期：2026-09-14  
工作包：WP-V13-01  
文档状态：`CANDIDATE`  
适用候选规则集：`v1.3-candidate-1`

> 本计划用于保存和比较证据，不产生交易许可。正式交易仍以[个人交易系统 v1.2](./last/个人交易系统_v1.2_基于ThesisGuard_v1.3.md)为准；A、C、行业确认和 Trend Hold 的结果均为 `SHADOW`。达到样本数量不会自动把 v1.3、Setup A/C、Trend Hold 或普通盈利加仓改成 `ACTIVE`。

候选定义见[个人交易系统 v1.3 产业趋势波段版 Candidate](./IterationRecord/个人交易系统_v1.3_产业趋势波段版_CANDIDATE.md)，设计依据见[v1.2 产业趋势中短期波段适配审计](./个人交易系统_v1.2_产业趋势中短期波段适配审计_2026-09-14.md)。

## 1. 要回答的问题

验证分为四个独立问题，不能用其中一个问题的好结果替另一个问题作结论：

1. **Setup A 是否补充了 B 系统性错过的首次启动段？** 比较 A 与 B 的净成本表现、假突破、持有期、市场阶段和资金占用。
2. **Setup C 是否提供独立的趋势中继机会？** 检查它是否只是晚周期、高波动的 A 类重复信号，或能形成可重复的第二段趋势。
3. **Setup B 的现行定义表现如何？** 保存所有触发和拒绝，不只保存实际成交，用它作为 ACTIVE BASELINE。
4. **Trend Hold 是否优于 v1.2 当前退出？** 在同一笔达到阶段条件的 NORMAL 交易上平行计算 v1.2 当前 5 日保护、完整 Trend Hold 和简化 10 日保护。

本计划不承诺证明策略盈利，也不把外部动量研究直接当作本系统参数有效性的证据。

## 2. 冻结与防止未来函数

每次验证运行必须登记：

```text
ValidationRunID / CandidateVersion / CodeVersion或计算表版本
UniverseDefinition / WatchlistSnapshotID / IndustryClassificationVersion
PriceDataSource / FundamentalDataSource / ConsensusSource
TradingCalendarVersion / CorporateActionMethod / FeeModelVersion / SlippageModelVersion
SignalStart / SignalEnd / DataAvailableAt / RunCreatedAt
Researcher / ReviewStatus / KnownLimitations
```

必须遵守以下边界：

- 历史日期只能使用当时已经发布且当时能够取得的数据；保存公告发布日期和实际取得时点。
- 使用时点化股票池和行业分类；退市、停牌、曾被移出股票池的证券不能从历史样本中无声消失。
- 信号用 t 日完整收盘数据，最早在 t+1 模拟执行；不得用 t+1 及以后的价格选择 t 日平台、目标、行业基准或证据状态。
- A、B、C、行业确认、过热、RED 和三条退出路径的版本先冻结。查看结果后改变参数，必须升级 `CandidateVersion` 并划分新的未见区间。
- 对同一证券、同一日期出现多个 Setup 时全部保存，并用 `OverlapGroupID` 标出重叠。不能在看到后续收益后只保留表现较好的类型。
- 任何 UNKNOWN、缺失、无法确定事件先后或无法还原合法成交的样本单独计数；不把它静默改成 FAIL、PASS 或零收益。

## 3. 数据集与唯一标识

建议使用五张逻辑表。可以先用 CSV、SQLite 或电子表格实现，但字段名和主键必须稳定。

| 数据集 | 一行代表 | 主键 | 用途 |
|---|---|---|---|
| `thesis_snapshot` | 某个信息截止时点的一张产业 Thesis 快照 | `ThesisSnapshotID` | 保存产业指标、预期、情景、催化与证伪 |
| `signal_log` | 某证券某日某 Setup 的一次判断 | `SignalID` | 保存 A/B/C 全部信号、连续变量、拒绝与重叠 |
| `entry_simulation` | 一个 Signal 的一套冻结执行假设 | `EntrySimulationID` | 保存 Pmax、S0、目标、合法数量、费用、滑点和是否可成交 |
| `exit_parallel_log` | 一笔入场在一种退出路径下的生命周期 | `PathID` | 比较 V12_CURRENT、TREND_HOLD、TEN_DAY |
| `follow_up_log` | 某路径退出后固定窗口的可比结果 | `PathID + Horizon` | 保存 20/60 交易日后续表现和机会成本 |

实际交易继续使用 v1.2 的 `TradeID`、`CampaignID`、`OrderID`。影子入场只使用 `SignalID` 和 `EntrySimulationID`，不得创建伪造的券商订单号或占用 EXPERIMENTAL 槽位。

## 4. A/B/C 信号日志

### 4.1 通用字段

每个研究范围内的证券，每个完整交易日，对达到 WATCH 以上或明确被规则拒绝的 A/B/C 判断保存：

```text
SignalID / CandidateVersion / SignalDate / DataAsOf
SecurityID / SecurityNameAtTime / Board / TradabilityStatus
SetupType = A | B | C
ComponentStatus = SHADOW | ACTIVE_BASELINE | SHADOW_DEFER
SetupStatus = WATCH | READY | TRIGGERED | FAIL | UNKNOWN | EXPIRED | INVALIDATED
ThesisSnapshotID / EvidenceStatus / ThesisInvalidationStatus
IndustryBenchmarkID / IndustryConfirmStatus / MarketFilterStatus / RedStatus
EventGate / AccountGate / OverheatedStatus
Pmax / S0 / Target / NetRR / LegalQuantity / HypotheticalRisk / FeeModelVersion
RejectionReasonPrimary / RejectionReasonsAll / MissingFields
OverlapGroupID / RecordedAt / SourceVersions
```

`TRIGGERED` 只表示相应 Setup 的冻结公式完成。真实可交易性还要经过 v1.2 的共同准入、市场、账户、价格、数量、事件和执行条件。A/C 即使全部通过，也只产生 SHADOW 记录。

### 4.2 Setup A 字段

```text
PlatformStart / PlatformEnd / PlatformHigh / PlatformLow / PlatformWidth
MA20_t / MA20_t5 / MA60_t / MA60_t10 / MA60Slope10
BreakoutPct / Volume_t / Prior20AvgVolume / VolumeRatio
EntryDayAbovePlatform / ShadowFillStatus / ExpiryReason
```

### 4.3 Setup B 字段

```text
PeakDate / PeakHigh / PullbackStart / PullbackEnd / PullbackLowDate / PullbackLow
PullbackPct / LowVsMA20Pct
PullbackAverageVolume / V12BaselineAverageVolume / V12VolumeRatio
EqualLengthPriorAverageVolume / EqualLengthVolumeRatioShadow
CloseVsPreviousHighPct / ActiveV12Decision
```

`EqualLengthVolumeRatioShadow` 只用于检查缩量基准敏感性，不改变 `ActiveV12Decision`。

### 4.4 Setup C 字段

```text
TrendStatus / SelectedN / PlatformStart / PlatformEnd
PlatformHigh / PlatformLow / PlatformWidth
BreakoutPct / PlatformAverageVolume / VolumeRatio / CloseVsMA20Pct
EntryDayAbovePlatform / ShadowFillStatus / ExpiryReason
```

N 必须按 20 向下到 7 的顺序自动选择满足条件的最大值。不得在后续表现已知后改用另一个平台长度。

## 5. 影子成交与成本

影子结果必须使用事前冻结且可重复的成交模型。首次建立数据表时，选择并锁定以下字段：

```text
OrderActivationTime / LimitPrice=Pmax / Validity=t+1
FillModel / SlippageModel / Commission / MinimumCommission / Taxes
PriceLimitRule / MinimumLot / QuantityStep / LiquidityCap
AmbiguousIntradaySequencePolicy
```

最低要求：

1. 不用 t+1 的最低价作为买价，也不用退出日最高价作为卖价。
2. 日线同时显示可能成交和先触发 S0、但无法确定盘中顺序时，主结果记 `AMBIGUOUS`；可以另报最不利边界，但不得把最有利顺序放入主结果。
3. 涨跌停、停牌、一字板、无成交、最小委托数量、T+1、公司行动、佣金最低收费和卖出税费按当时适用规则处理。
4. A/C 的 `HypotheticalRisk` 只用于比较，不进入真实 H、CashPool、账户回撤或实验仓统计。
5. Setup 间比较同时报告“全部 TRIGGERED 信号”和“按冻结模型可成交信号”，防止把执行困难隐藏成选股失败或成功。

## 6. 当前退出与 Trend Hold 平行结果

### 6.1 比较对象

每笔符合条件的 NORMAL 实际交易，或按统一影子成交模型形成的可比较 B 入场，同时建立三个 `PathID`：

| `ExitPath` | 定义 | 状态 |
|---|---|---|
| `V12_CURRENT` | v1.2 当前实际盈利保护与退出规则 | `ACTIVE BASELINE` |
| `TREND_HOLD` | v1.3 Candidate 的分阶段 Trend Hold | `SHADOW` |
| `TEN_DAY` | +2r 后使用 10 日低点，其余共同退出不变 | `SHADOW` 敏感性对照 |

三条路径固定使用同一 Entry0、数量、原 S0、r0、R0、公司行动转换、费用模型、Thesis 证伪和账户强制退出信息。路径差异只允许来自盈利保护算法。

### 6.2 事件优先级

每天按以下顺序推进每条路径：

1. 公司行动换算与数据完整性检查；
2. v1.2 共同账户强制退出、Thesis 证伪及已锁定事件退出；
3. 该路径上一交易日生成并于当日生效的保护价；
4. 当日收盘后的 +1r、+2r、OVERHEATED 和次日保护价计算；
5. 到期、未成交、停牌或价格限制状态。

如果真实 V12_CURRENT 已退出，仍要按事前方案继续维护 SHADOW 路径所需的公开数据和 Thesis/账户事件，直到影子退出或最长观察期结束。无法继续取得一致的事件判断时，影子路径标 `UNKNOWN_AFTER_ACTUAL_EXIT`，不能假设 Thesis 永远有效。

### 6.3 每条退出路径字段

```text
PathID / TradeID或EntrySimulationID / ExitPath / CandidateVersion
EntryDate / Entry0 / Quantity / S0 / r0 / R0
Stage / StageChangedAt / EffectiveStop / StopGeneratedAt
OverheatedTrigger / CommonExitEvent / ExitTrigger / ExitDecisionAt
ExecutableExitAt / ExitPrice / ExitCosts / NetPnL / FinalR
HoldingTradingDays / CapitalDays / PathComplete / UnknownReason
MFE_R / MAE_R / Hit1R / Hit2R / MaxOpenProfitR / MaxGivebackR
```

## 7. MFE 与 MAE

对每条完整路径使用该路径从成交到退出可执行时点的同尺度日线。若只有日线且同日同时出现更高高点和退出触发，MFE/MAE 可以按日内极值记录，但成交顺序不明时另标 `INTRADAY_ORDER_UNKNOWN`。

```text
MFE_R = max(0, [持有路径内最高可比High - Entry0] / r0)
MAE_R = max(0, [Entry0 - 持有路径内最低可比Low] / r0)
MaxOpenProfitR = max(0, 逐日按当时可执行退出成本估算的OpenPnL / R0)
MaxGivebackR = max(0, MaxOpenProfitR - FinalR)
```

- `r0` 是元/股尺度；`R0` 是整笔金额尺度，不混用。
- MFE/MAE 不扣费用，净盈亏和 FinalR 扣除真实或冻结成本；报表必须明确口径。
- r0、R0、复权换算或价格序列不可核实时，对应指标为 UNKNOWN，并披露缺失笔数。
- 部分成交和部分退出归入同一 Trade/Campaign，不能拆成多笔来提高胜率或改变 MFE/MAE。

## 8. Expectancy 与 Profit Factor

### 8.1 单笔结果

```text
NetPnL = 全部退出现金流 + 股息等归属现金流 - 全部入场现金流 - 全部费用税费
FinalR = NetPnL / R0
```

只有 R0 有效且路径完整时才进入 R 倍数主样本。金额结果始终保留；R0 缺失不能事后用实际亏损或预算倒算。

### 8.2 汇总

```text
Expectancy_R = sum(FinalR_i) / N_valid_R
WinRate = N(NetPnL>0) / N_complete_amount
AverageWinR = mean(FinalR_i where FinalR_i>0)
AverageLossR = abs(mean(FinalR_i where FinalR_i<0))
ProfitFactor_Amount = sum(NetPnL_i where >0) / abs(sum(NetPnL_i where <0))
ProfitFactor_R = sum(FinalR_i where >0) / abs(sum(FinalR_i where <0))
```

Profit Factor 以金额口径为主，R 口径另列，不混写。有盈利但没有亏损分母时显示“无亏损分母”，无完整样本或全为零时显示“不适用”，不得填任意大数。所有报表同时展示完整数、开放数、UNKNOWN 数、违规数和被排除原因。

除平均数外至少展示中位数、25/75 分位数、最大连续亏损、最大回撤、平均持有日、CapitalDays、+1R/+2R 比例与每年/市场阶段分布。样本小或高度集中于同产业同期时，结论标为探索性。

## 9. 退出后 20/60 交易日表现

对每条完成退出路径，以退出后第一个完整交易日为第 1 日，使用相同复权尺度保存：

```text
FollowUpReturn20 = Close(exit+20) / ComparableExitPrice - 1
FollowUpReturn60 = Close(exit+60) / ComparableExitPrice - 1
PostExitMax20R = max(0, [maxHigh(exit+1…exit+20) - ComparableExitPrice] / r0)
PostExitMax60R = max(0, [maxHigh(exit+1…exit+60) - ComparableExitPrice] / r0)
PostExitMin20R / PostExitMin60R = 同期最低价相对ComparableExitPrice的R变化
MissedMove20R = PostExitMax20R
SoldEarly20 = 盈利保护退出且MissedMove20R>=2
```

`ComparableExitPrice` 必须使用统一、可执行的退出价格模型，并保存费用是否已计入。停牌或不足 20/60 个后续交易日时状态为 `PENDING`；退市、长期停牌或数据中断单列，不能删除。

20 日是主要卖飞诊断窗口，60 日为次要产业趋势观察窗口。降低 `SoldEarly20` 比例不能单独证明 Trend Hold 更好；仍需同时比较净成本 Expectancy、Profit Factor、最大回撤、资金占用和执行错误。

## 10. 分组与重叠分析

主报告先展示总样本，再展示预先登记的诊断分组：

- Setup A/B/C 与 A-B、A-C、B-C 同日重叠；
- 产业、行业基准和同一产业同期聚类；
- NORMAL 市场过滤 PASS/FAIL/UNKNOWN 与 GREEN/AMBER/RED；
- 年份、波动阶段、证据新鲜度和初始估值分位；
- 可成交/不可成交、合规/违规、真实/影子；
- ExitPath、是否达到 +1r/+2r、是否触发 OVERHEATED。

小组只用于诊断。不得根据很小子组临时新增特批规则；提出的新分组规则需要新的 CandidateVersion 和未见验证区间。

## 11. 四阶段验证

| 阶段 | 方法 | 最低工作量与完成条件 | 能否自动进入下一状态 |
|---|---|---|---|
| 定义核验 | 用手工边界案例检查窗口、边界、UNKNOWN、公司行动、费用和事件顺序 | 每个公式有边界例；相同输入可重复得到相同结果 | 否 |
| 历史重演 | 跨产业、市场阶段和年份，使用时点化数据 | 争取每种 Setup 至少 60 个历史 TRIGGERED；不足时如实报告，不凑数 | 否 |
| Walk-forward | 按时间滚动冻结参数，在未见区间计算 | 每种 Setup 至少 30 个完整未见信号作为最低工作目标；无未来数据和版本混用 | 否 |
| 前瞻 SHADOW | 每日保存全部 WATCH/READY/TRIGGERED、拒绝及假设成交 | 每种候选 Setup 至少 20 个完整前瞻信号；记录覆盖率 100% | 否 |

上述数量是最低工作目标，不是统计显著性、盈利证明或实盘授权。A 若通过审查，未来最多只能在新的正式决议中讨论 0.5 倍 NORMAL 预算试行；C 仍为 DEFER；Trend Hold 只有在单独退出决议后才能替换实际保护。

## 12. 决策报告模板

每次阶段评审生成同一模板：

```text
ReviewID / CandidateVersion / DataCutoff / ValidationStage
样本：Triggered / Fillable / Complete / Open / Unknown / Violations
Setup A/B/C：Expectancy_R / PF_Amount / WinRate / AvgWinR / AvgLossR
路径：V12_CURRENT / TREND_HOLD / TEN_DAY 的同笔差值及置信区间或重采样区间
风险：MaxDrawdown / MaxLosingStreak / MAE_R / MaxGivebackR
效率：HoldingDays / CapitalDays / SignalsPerYear / Overlap / Concentration
后续：FollowUp20 / FollowUp60 / SoldEarly20
数据质量：Missing / Survivorship / PointInTime / CorporateActions / Ambiguity
行为：漏记 / 事后改参数 / 延迟止损 / 下移止损 / 超计划数量
Decision = KEEP_SHADOW | REVISE_NEW_VERSION | REJECT | PROPOSE_PILOT
Reviewer / HumanApproval / DecisionDate
```

只有 `PROPOSE_PILOT` 也不能直接执行；它只是提交人工决议。正式状态、风险预算、启用日期、旧仓迁移和回滚方式必须在新的批准记录中明确。

## 13. 当前初始化状态

截至本计划建立时：

| 项目 | 状态 |
|---|---|
| 历史信号数据 | 尚未导入 |
| 前瞻 A/B/C 信号 | 0 |
| 完整退出平行样本 | 0 |
| MFE/MAE 有效样本 | 0 |
| 20/60 日成熟样本 | 0 |
| v1.3 实盘授权 | 无 |
| ADR-001 人工决定 | 待确认 |

任何未来数据追加都应保留原始记录和版本，不覆盖已经用于决策的快照。
